IMAGINE

This is a GPU accelerated image editor. Loads BMP and TGA file format images as a GPU texture, and has a selection of image effects that can be applied using shader hardware directly on the GPU. The final image can be saved to a TGA file.

Runs on Windows, uses OpenGL.

Built with Visual studio 2008.